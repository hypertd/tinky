#!/usr/bin/env php
<?php

require_once '/home/vagrant/.composer/vendor/psy/psysh/bin/psysh';

/**
 * Add breakpoints using \Psy\Shell::debug(get_defined_vars());
 *
 */


$config = (include __DIR__.'/Config.php');

if($config['bootstrapFile']){
	require __DIR__.'/Bootstraps/'.$config['bootstrapFile'];
}

$config = new \Psy\Configuration();
$config->loadConfigFile(__DIR__.'/Config.php');

$shell = new \Psy\Shell($config);
$shell->run();
